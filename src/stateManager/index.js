export default class {
  pool = [];
  constructor(pool) {
    this.pool;
  }

  //   globalListen(){
  //     Object.keys(window).forEach(key => {
  //       if (/^on/.test(key)) {
  //           window.addEventListener(key.slice(2), event => {
  //               console.log(event);
  //           });
  //       }
  //   });
  //   }
}